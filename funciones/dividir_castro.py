#funciones/dividir_castro.py

def dividir_castro(a, b):
    """Devuelve la división de dos números. Si b es 0, devuelve None."""
    
    # Esta línea DEBE estar indentada (con 4 espacios)
    if b == 0:
        # Esta línea DEBE estar indentada (con 8 espacios)
        return None
    
    # Esta línea DEBE estar indentada (con 4 espacios)
    return a / b